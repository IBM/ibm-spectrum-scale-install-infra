---
# Task to setup the Auth

- name: Setup Auth
  shell: "/usr/lpp/mmfs/bin/mmuserauth service create --data-access-method file --type userdefined"
  ignore_errors: yes
  when: is_admin_node | default(false) == true and service_result.rc != 0
  run_once: true