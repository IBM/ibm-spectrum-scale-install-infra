# ---

# # Key Protect preparation
# - import_tasks: create_trust_cert.yml

# # Get Encryption files
# - import_tasks: get_encryption_files.yml

# # Apply Encryption
# - import_tasks: apply_encryption.yml

---

# Key Protect preparation on a specific server
- hosts: scale_nodes[2]
  tasks:
    - import_tasks: create_trust_cert.yml

# Get Encryption files on all nodes in a group
- hosts: scale_nodes
  tasks:
    - import_tasks: get_encryption_files.yml

# Apply Encryption on a single node
- hosts: scale_nodes[2]
  tasks:
    - import_tasks: apply_encryption.yml
