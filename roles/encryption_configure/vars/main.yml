#Static Variables for Encryption

#Directory to store SGKLM files
sgklm_dir: "/opt/sgklm"

#Tenant name
tenant_name: "{{ scale_cluster_clustername | regex_replace('[^A-Za-z0-9]', '') }}"

#Password file for performing Encryption operations
password_file: "{{ sgklm_dir }}/.sgklm_pass"