---
# Registering the client to the created tenant for the storage cluster

# Make default variables available in hostvars

- name: "************************ Registering the Storage Client to the tenant ************************"
  command: mmkeyserv client register "{{ client_name }}" --rkm-id rkm_ClusterTenant1 --tenant ClusterTenant1 --server-pwd "{{ password_file }}"
  register: register_client_output
  run_once: true

- debug:
    var: register_client_output.stdout_lines
  