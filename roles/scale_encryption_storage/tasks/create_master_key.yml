---
# Creating Master key for encryption

# Make default variables available in hostvars

- name: "************************ Creating Master Key for Encryption ************************"
  command: mmkeyserv key create --server "{{ scale_encryption_servers[0] }}" --tenant ClusterTenant1 --server-pwd "{{ password_file }}"
  register: master_key_output
  run_once: true