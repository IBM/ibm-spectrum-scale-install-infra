- name: Ensure password is set if not found
  replace:
    path: "{{ config_file }}"
    regexp: '^#\s*(password\s*=\s*TXlWZXJ5U3Ryb25nUGFzc3cwcmQhCg==)'
    replace: "password = {{ scale_grafana_bridge.encoded_password }}"
  when:
    - config_stat.stat.exists
