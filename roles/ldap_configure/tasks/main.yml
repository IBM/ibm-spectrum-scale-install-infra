---

    # Integration LDAP with IBM Spectrum Scale (GPFS) for user level access.
    # Below are the LDAP user integration and LDAP integration on cluster tasks.
        # Import the 'prerequesites.yml' task for installing the required packages on the client servers for LDAP.
        - import_tasks: prerequesites.yml

        # Import the 'user_integration.yml' task for authenticating LDAP users to access the cluster nodes.
        - import_tasks: user_integration.yml

        # Import the 'cluster_integration.yml' task for authentication LDAP users to perform cluster level operations.
        - import_tasks: cluster_integration.yml